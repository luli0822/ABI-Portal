describe('client module', () => {
	it('Check getClient() and fetchSiteData() will return the expected site Data from contentful ', async () => {
		// TODO: test1: mock contentful API call in getClient(). Mock expected output and verify.
		// TODO: test2: Check fetchSiteData() is called and returns staticSiteData

		expect(1).toBe(1);
	});
});
